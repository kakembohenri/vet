import spinner from "./spinner1.gif";

const Spinner = () => {
  return (
    <div style={{ height: "3rem" }}>
      <img style={{ height: "100%" }} src={spinner} alt='spinner gif' />
    </div>
  );
};
export default Spinner;
